<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rr.division_of_bangladesh">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DhakaActivity" />
        <activity android:name=".CtgActivity" />
        <activity android:name=".KhulnaActivity" />
        <activity android:name=".RajshahiActivity" />
        <activity android:name=".SylhtActivity" />
        <activity android:name=".BarisalActivity" />
        <activity android:name=".RangpurActivity"></activity>
    </application>

</manifest>